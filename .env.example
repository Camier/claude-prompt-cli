# Hugging Face API Token for AI Enhancement
# Get your token from: https://huggingface.co/settings/tokens
# Requires a HF Pro account for better rate limits

HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional: Set default AI model tier (fast, balanced, deep)
# DEFAULT_AI_MODEL=balanced

# Optional: Set cache directory for persistent storage
# CACHE_DIR=~/.cache/claude-enhancer
# Note: Cache is now persistent across restarts!
# Cache files: cache-data.json (entries) and cache-meta.json (statistics)